modules = ["nodejs-20", "postgresql-16"]
[deployment]
build = ["sh", "-c", "npm run build && npx vite build"]
run = ["sh", "-c", "npm run start"]
deploymentTarget = "autoscale"

[nix]
channel = "stable-25_05"

[[ports]]
localPort = 3000
externalPort = 80

[agent]
integrations = ["resend:1.0.0"]
